const baseURL = 'https://jsonplaceholder.typicode.com'

const users = '/users'
const comments = '/comments'

const urls = {
    users: users,
    comments: comments
}

export {
    baseURL,
    urls
}