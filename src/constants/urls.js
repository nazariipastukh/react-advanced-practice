const carsURL = 'http://owu.linkpc.net/carsAPI/v1'
const jsonURL = 'https://jsonplaceholder.typicode.com'

const cars = '/cars'
const users = '/users'
const posts = '/posts'

const urls = {
    cars,
    users,
    posts
}

export {
    carsURL,
    jsonURL,
    urls
}